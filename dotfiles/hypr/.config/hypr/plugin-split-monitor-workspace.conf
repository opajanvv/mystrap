# split-monitor-workspaces plugin configuration
# 4 workspaces per monitor with persistent workspaces enabled

plugin {
    split-monitor-workspaces {
        count = 4
        keep_focused = 0
        enable_notifications = 1
        enable_persistent_workspaces = 1
    }
}
